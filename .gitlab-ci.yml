image: tmaier/docker-compose:latest

services:
  - docker:dind

before_script:
  - docker info
  - docker-compose --version

tests:
  stage: tests
  tags:
    - docker
  script:
    - cd docker && docker-compose run tests
